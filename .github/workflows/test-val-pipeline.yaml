---
name: Test the validation pipeline reusable workflow

on:
  pull_request:
    branches:
      - main

jobs:
  test-val-pipeline-buckbeak:
    uses: ./.github/workflows/val-pipeline.yaml
    with:
      target: buckbeak
      report_pkg_dir: ./tests/packages/buckbeak
